import React from 'react';

const UserOrder = () => {
    return (
        <div>
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
            this is usr order page
        </div>
    );
};

export default UserOrder;